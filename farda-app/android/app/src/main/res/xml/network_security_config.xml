<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security policy for the farda. app.

  Issue #20: the app must talk to its backends over HTTPS only.

  - The base config below disallows ALL cleartext (plaintext http://) traffic,
    so production / staging API calls are forced onto TLS.
  - The single domain exception scopes a cleartext allowance to local
    development hosts only:
      * 10.0.2.2  -> the host machine as seen from the Android emulator
      * localhost / 127.0.0.1 -> for adb reverse / on-device local servers
    These are never reachable in a shipped build, so this exception cannot
    weaken production traffic.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
